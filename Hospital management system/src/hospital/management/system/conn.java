package hospital.management.system;import java.sql.*;public class conn {    public Connection connection;    public Statement statement;    public conn() {        try {            Class.forName("com.mysql.cj.jdbc.Driver"); // Make sure you added the connector jar            connection = DriverManager.getConnection("jdbc:mysql://localhost:3306/hospital__management__system","root", "STR@#765wx89");   // database name                       // password (empty if you haven't set one)            statement = connection.createStatement();        } catch (Exception e) {            System.out.println("Database Connection Failed!");            e.printStackTrace(); // This will show exactly why the connection failed        }    }}